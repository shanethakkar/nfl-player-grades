import { ClipScene } from "./ClipScene";

/**
 * Clip 3 (4s) — Per-component breakdown on a player profile: the
 * stats and weights that produce the grade. Single, focused idea —
 * the grade-explainability differentiator.
 */
export function Clip3Scene() {
  return (
    <ClipScene
      src="clip-3.mp4"
      kicker="Transparent by design"
      caption="See every stat behind the grade."
    />
  );
}
